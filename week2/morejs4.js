function vehicleType (color, code ){
if (code === 1) {
console.log('a ' + color + ' car');
 }
 if (code === 2) {
    console.log('a ' + color + ' motorbike');
     }
}
vehicleType('red', 1);
vehicleType('blue', 2);